name: scooter_flutter
description: Shared Flutter and device integration for scooter applications.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.5.0 <4.0.0'
  flutter: '>=3.38.6'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.34.5
  crypto: ^3.0.3
  logging: ^1.2.0
  shared_preferences: ^2.2.2
  scooter_core:
    path: ../scooter_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
